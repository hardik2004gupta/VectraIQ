# UPDATED_OPENAPI_SUMMARY.md — VectraIQ Phase 3

The OpenAPI schema is auto-generated by FastAPI and available at:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`
- Raw JSON: `http://localhost:8000/openapi.json`

---

## Application Info

```json
{
  "title": "VectraIQ",
  "version": "2.0.0",
  "description": "Production-grade AI Knowledge Platform — Kubernetes IT-Operations Copilot with Hybrid RAG, Text2SQL, Intelligent Routing, and Enterprise Security."
}
```

---

## Tag Groups

| Tag | Description |
|---|---|
| `query` | AI-powered question answering via RAG, Text2SQL, and Hybrid pipelines. |
| `auth` | User registration and authentication (JWT bearer tokens). |
| `admin` | Health checks, cache management, and operational endpoints. Requires admin role. |

---

## Endpoints

### POST /query

```yaml
summary: Submit a natural language question
tags: [query]
security: [BearerAuth]
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/QueryRequest'
responses:
  200:
    description: Answer generated successfully
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ChatResponse'
  400:
    description: Input blocked by security scan
  401:
    description: Missing or invalid JWT token
  429:
    description: Rate limit or daily token budget exceeded
```

### POST /query/sql/execute

```yaml
summary: Approve or reject a pending SQL query
tags: [query]
security: [BearerAuth]
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/SqlApprovalRequest'
responses:
  200:
    description: SQL executed or cancelled
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ChatResponse'
  401:
    description: Missing or invalid JWT token
```

### POST /auth/register

```yaml
summary: Register a new user account
tags: [auth]
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/AuthRequest'
responses:
  201:
    description: User registered, JWT returned
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/TokenResponse'
  409:
    description: Username already exists
  429:
    description: Registration rate limit exceeded
```

### POST /auth/login

```yaml
summary: Authenticate and receive a JWT bearer token
tags: [auth]
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/AuthRequest'
responses:
  200:
    description: JWT returned
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/TokenResponse'
  401:
    description: Invalid credentials
  429:
    description: Login rate limit exceeded
```

### GET /admin/health

```yaml
summary: Check health of all dependencies
tags: [admin]
responses:
  200:
    description: Health status of all dependencies
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ServiceHealth'
```

### GET /admin/cache/stats

```yaml
summary: Per-tier cache hit/miss statistics
tags: [admin]
security: [BearerAuth]
responses:
  200:
    description: Cache statistics for all five tiers
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/CacheStatsResponse'
  401:
    description: Admin authentication required
  403:
    description: User is not an admin
```

### POST /admin/cache/clear

```yaml
summary: Clear the in-memory cache and reset statistics
tags: [admin]
security: [BearerAuth]
responses:
  200:
    description: Cache cleared
  401:
    description: Admin authentication required
  403:
    description: User is not an admin
```

---

## Key Schemas

### QueryRequest

| Field | Type | Required | Default | Constraints |
|---|---|---|---|---|
| `question` | string | ✅ | — | 1–2000 chars |
| `top_k` | integer | — | 5 | 1–50 |
| `search_mode` | "dense"\|"sparse"\|"hybrid" | — | "dense" | — |
| `enable_rerank` | boolean | — | false | — |
| `enable_hyde` | boolean | — | false | — |
| `enable_crag` | boolean | — | true | — |
| `enable_self_reflective` | boolean | — | false | — |

### ChatResponse

| Field | Type | Nullable | Description |
|---|---|---|---|
| `answer` | string | — | Generated answer |
| `sources` | string[] | — | Source document names |
| `confidence` | float | — | 0.0–1.0 |
| `pending_sql` | PendingSQLBlock | ✅ | Present when SQL approval needed |
| `cache_hit` | boolean | — | True if served from cache |
| `request_id` | string | — | Correlation ID |
| `metadata` | ResponseMetadata | — | Retrieval and reflection details |

### AuthRequest

| Field | Type | Required | Constraints |
|---|---|---|---|
| `username` | string | ✅ | 3–64 chars |
| `password` | string | ✅ | 8–128 chars |

### TokenResponse

| Field | Type | Description |
|---|---|---|
| `token` | string | JWT bearer token |
| `token_type` | string | Always "bearer" |
| `expires_in` | integer | TTL in seconds |

### ErrorResponse (all error responses)

```json
{
  "error": {
    "code": "string",
    "message": "string",
    "details": {}
  },
  "request_id": "string"
}
```

---

## OpenAPI Improvements in Phase 3

| Before | After |
|---|---|
| No tag descriptions | Tag descriptions added in `openapi_tags` |
| `SqlExecuteRequest` inline class (not in schema) | `SqlApprovalRequest` in `models.py` (appears in schema) |
| Auth returned bare `{"token": "..."}` dict | Returns typed `TokenResponse` (in schema) |
| Admin used untyped `dict` returns | `ServiceHealth`, `CacheStatsResponse`, `CacheTierStats` in schema |
| No `summary` or `description` on endpoints | All endpoints have `summary` + `description` |
| No `responses=` metadata | Error responses documented per endpoint |
| No app description | Full description in FastAPI constructor |
| `ChatRequest` unused model in schema | Removed (never used in a route) |
